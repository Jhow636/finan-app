import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  background: {
    flex: 1,
    backgroundColor: '#f0f4ff',
    paddingTop: 30,
  },

  listBalance: {
    maxHeight: 190,
  },
});
